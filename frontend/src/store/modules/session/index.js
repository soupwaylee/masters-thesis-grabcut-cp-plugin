const state = {
  testImages: [1, 2],
  currentImageIndex: 0,
};

const mutations = {
};

const actions = {
};

const getters = {
};

const interactionSessionModule = {
  state,
  mutations,
  actions,
  getters,
}

export default interactionSessionModule;